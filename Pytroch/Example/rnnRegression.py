import torch.nn as nn
import torchvision.datasets as dataSet
import torchvision.transforms as transforms
import matplotlib.pyplot as plt
import numpy as np
TIME_STEP = 10
INPUT_SIZE = 1
LR = 0.02

steps = np.linspace(0, np.pi * 2,)
np.linspace(1, 1)
